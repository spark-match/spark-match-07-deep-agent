"""Tests for content of skill files under skills/."""

# Marker module so pytest collects tests in this package as a proper
# sub-package (avoids basename collisions with same-named test modules in
# other tests/<domain>/ directories) — same rationale as tests/mcp/__init__.py.
